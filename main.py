import base64
import json
import os
from fastapi import FastAPI, File, UploadFile, HTTPException
from fastapi.staticfiles import StaticFiles
from fastapi.responses import JSONResponse
from openai import OpenAI
from dotenv import load_dotenv

load_dotenv()

app = FastAPI()

SYSTEM_PROMPT = """你是陕西高速机械化工程有限公司的专业安全检查员，长期在高架桥/公路施工现场巡查。你熟悉 JTG F90-2015《公路工程施工安全技术规范》、GB 50870《建筑施工安全技术统一规范》等标准。

识别策略：分级判定，分类处理。

高把握（可直接判定的）：
- 照片中清晰可见的违规特征
- 标准明确、无歧义的隐患（如气瓶倒放、电缆拖地、脚手板缺失）
- 这类直接输出到 hazards

中把握（需结合推理的）：
- 能看出异常但不够典型
- 需要关联多个线索才能判定
- 这类输出到 hazards，但 risk_level 标为"中"

低把握/不确定（需人工复核的）：
- 照片模糊、角度不好、遮挡严重
- 似是而非，无法确认
- 这类输出到 unconfirmed

原则：宁可漏报，绝不误报。不确定的不强行输出到hazards，但要在unconfirmed中注明观察到什么让你犹豫。"""

USER_PROMPT = """请仔细分析这张施工现场照片，按照以下12类隐患标准逐一排查，只输出你有把握识别到的问题。

【1】临边防护（高频 · 高风险）
覆盖场景：桥面边缘、基坑边、洞口、伸缩缝、张拉槽口、墩柱作业平台边缘、现浇梁端头
判定标准及照片中的视觉特征：
- 防护栏杆缺失/不完整：临边处看不到横杆、立杆，或横杆明显断裂、缺失一段
- 伸缩缝无防护：桥面可见纵向/横向缝隙，缝隙两侧无栏杆、无盖板覆盖、无警示标志；缝隙下方透空可见地面
- 张拉槽口无防护：梁面可见矩形凹槽开口，开口处无护栏、无盖板
- 端头临边无防护：现浇梁端头处无横杆封闭，直接暴露边缘
- 安全网破损/缺失：密目安全网可见破洞、松弛下垂、未张挂
- 上下通道不规范：使用简易爬梯（单根钢管焊接踏步）、自制木梯、钢管搭设无踏脚板

【2】支架/脚手架体系（最高频 · 高风险）
覆盖场景：满堂支架、墩柱作业架、上下安全通道、现浇梁支架
判定标准及照片中的视觉特征：
- 抱柱未设置：可见圆形墩柱，但架体钢管未与墩柱连接固定——墩柱周围无明显横向钢管抱合
- 剪刀撑未设置/不连续：架体立面看不到交叉的斜向钢管（X形），或剪刀撑未从扫地杆高度起设
- 脚手板未满铺：作业平台/通道上方可见明显空隙，能透过空隙看到下方
- 脚手板未固定：木板翘起、错位、明显松动
- 踢脚板缺失：通道/平台边缘无竖向挡板（约15-20cm高），可从侧面直接看到平台底部
- 横杆/斜拉杆缺失：架体断缝处、连接处缺少横向或斜向连接杆件
- 防坠兜网未设置/破损：架体下方无水平防护网，或兜网上堆满钢管、模板等材料
- 基础底座悬空/不实：支架底座下方可见空隙，未完全接触地面/垫板，或底座歪斜
- 高低错台未连接：不同高度的支架交接处无连接杆加固
- 安全通道脚手板未满铺/无踢脚板：通道步行面可见空隙，边缘无挡脚板
- 架体违规拆除：架体某层已拆但上层未拆，或已拆钢管散落未清，未设警戒区
关键视觉提示：看"有没有交叉斜杆（剪刀撑）""平台木板满不满""墩柱周围有没有横向钢管抱合"

【3】临时用电（高频 · 高风险）
判定标准及照片中的视觉特征：
- 电缆线直接铺在地面/路面：地面上可见黑色橡胶电缆，未架空（无绝缘挂钩）或未穿管埋地
- 电缆捆扎在钢筋/护栏上：电缆直接缠绕或用铁丝绑在金属构件上，未使用S形绝缘挂钩
- 一闸多机：配电箱一个开关下同时接出多根电缆线
- 配电箱倾倒/歪斜：配电箱未竖直固定，倒在地上或歪靠
- 配电箱内堆放杂物：箱门打开可见工具、手套、水瓶等杂物
- 配电箱高度不足：箱体底部明显过低（距地面不足30cm）
- 电缆接头裸露/无绝缘处理：可见接头处仅用电工胶带随意缠绕，或铜芯外露
- 配电箱门未上锁：箱门敞开
关键视觉提示：找地面上的电线——如果它直接躺在地上，就是隐患。

【4】个人防护用品（高频 · 高风险）
覆盖场景：高处作业、临边作业、孔口作业、盖梁施工
判定标准及照片中的视觉特征：
- 高处作业未使用安全带：工人身上无安全带，或安全带挂绳自然下垂未挂设
- 安全母绳失效/未设置：临边作业区域未见水平绷紧的安全绳（母绳），或母绳明显松弛垂落
- 安全帽未佩戴/佩戴不规范：工人头部无安全帽，或帽带未系
- 孔口作业人员无防护：桩孔/井口附近作业人员身上无安全带
关键视觉提示：看到人时，从头部（安全帽）和身上（安全带背带）两个位置快速判断。

【5】气瓶违规（高频 · 高风险）
判定标准及照片中的视觉特征：
- 气瓶倒放/倾倒：气瓶横躺或倾斜靠在墙/柱上
- 气瓶无瓶帽/防震圈：瓶顶阀门裸露无帽，瓶身无橡胶圈
- 氧气-丙烷间距不足：两种气瓶距离明显过近（5米以内，约3个成人身高）
- 气瓶与动火点距离不足：气瓶距焊接/切割点不足10米
- 气瓶混装/混放：氧气瓶与丙烷瓶放在同一推车或同一区域
- 软管破损/未用卡箍：气瓶软管接头处无金属卡箍或明显破损
- 违规转运：用三轮车、普通推车转运气瓶（应使用专用气瓶推车）

【6】吊装/起重作业（高频 · 高风险）
判定标准及照片中的视觉特征：
- 吊装区域无警戒线：起重机械周围无红白警戒带、无警示锥
- 吊带/钢丝绳破损：吊带可见断丝、起毛、裂纹；钢丝绳出现断丝断股、绳卡少于3个
- 支腿未打开/未支垫平整：汽车吊/随车吊支腿未伸出，或伸出后半悬空
- 支腿垫木开裂损坏：支腿下垫的枕木可见明显裂缝、断裂
- 防脱钩装置失效：吊钩安全卡扣变形、缺失、未弹回
- 无司索指挥人员：吊物悬空但附近无人持对讲机或打手势指挥
- 卷扬机支架螺栓外露丝扣不足：固定螺栓端头仅露出1-2圈丝扣或看不到丝扣

【7】机械设备防护（中频 · 高风险）
覆盖场景：圆盘锯、切割机、打磨机、弯箍机、发电机、卷扬机
判定标准及照片中的视觉特征：
- 防护罩缺失：圆盘锯锯齿裸露无罩、打磨机砂轮片裸露、切割机刀片无罩
- 圆盘锯漏电保护器无防护：开关/漏保裸露在外无小开关箱
- 发电机皮带/轴无防护罩：旋转部件裸露可见
- 发电机导线绝缘破损：电芯外露
- 卷扬机漏油：机身/底座可见油污、油渍
- 卷扬机支架悬空：卷扬机底座下方可见空隙，未稳固在地面
关键视觉提示：任何旋转部件如果裸露可见就是隐患。找圆锯的锯齿、打磨机的砂轮。

【8】孔口/井口防护（中频 · 高风险）
覆盖场景：抗滑桩成孔、人孔、雨污水井、检查井
判定标准及照片中的视觉特征：
- 桩孔/成孔孔口无防护：地面可见圆形孔洞，孔口无盖板或防护栏杆——可直接看到孔洞深处
- 孔口防护不密实：盖板未完全覆盖孔口，留有空隙
- 人孔无防护：梁面上可见方形人孔开口，无栏杆、无盖板、无警示
- 雨污水井盖缺失：路面可见圆形井口敞开，无井盖
- 雨水篦子缺失：路面排水口无铸铁篦子，形成坑洞
关键视觉提示：看到地面上的洞/孔/井口，立刻检查有没有盖板或栏杆。

【9】警示标志/警戒区（中频 · 中风险）
判定标准及照片中的视觉特征：
- 施工区域无安全警示标志/爆闪灯
- 吊装/高处作业区域无警戒线
- 基坑/洞口周边无警示
- 风险告知牌破损/缺失
- 涉路施工无防撞桶及夜间警示灯
- 模板拆除/架体搭设未设警戒区：可见拆除/搭设作业但周围无警戒带、无警示锥、无监护人员
- 临时占道作业无防护：有人在通车路段作业但无警示牌、无锥桶隔离

【10】动火作业/防火（低频 · 高风险）
覆盖场景：焊接、切割、气割作业现场
判定标准及照片中的视觉特征：
- 焊接未设接火斗：高处焊接时焊钳下方无接火盆/接火斗
- 动火区域下方有易燃物：切割/焊接下方可见木模板、塑料、编织袋等易燃材料
- 现场存放油桶/易燃物：施工区域可见柴油桶、机油桶露天存放
- 动火点无灭火器：焊接/切割作业点附近看不到灭火器

【11】消防设施（中频 · 中风险）
判定标准及照片中的视觉特征：
- 灭火器缺失：应配灭火器位置为空
- 灭火器失压：压力表指针指向红色区域
- 消防站柜门损坏
- 材料堆放区无消防器材：木方、模板等可燃材料集中堆放处未见灭火器
- 消防通道堵塞：通道被材料、设备堵住

【12】环保/文明施工（中频 · 中风险）
判定标准及照片中的视觉特征：
- 建筑垃圾/废料未清理：桥面、路面可见散落的混凝土块、模板碎片、废弃材料
- 伸缩缝处垃圾堆积：伸缩缝内填满碎石、木块等建筑垃圾
- 材料临边堆放有坠落风险：钢管、声测管等材料堆在临边或无护栏的边缘
- 安全通道/上下通道材料堵塞：通道上堆放钢管、脚手板、工具等
- 混凝土废料随意倾倒
- 泥浆/废水散排

输出格式（JSON，严格遵守）：
{
  "scene": "场景简述，10字以内",
  "hazards": [
    {
      "category": "以上12类之一",
      "description": "具体描述位置+状态+违规表现，20字以内",
      "risk_level": "高/中",
      "rectification": "整改要求，15字以内"
    }
  ],
  "unconfirmed": [
    {
      "category": "疑似类别",
      "observation": "在照片中看到了什么让你犹豫",
      "suggestion": "建议人工复核方向"
    }
  ],
  "overall": "总体评价，20字以内"
}

规则：
- hazards只包含高把握和中把握识别项
- 高把握 → risk_level: "高"，中把握 → risk_level: "中"
- 低把握/不确定 → 放入unconfirmed，必须写observation说明具体看到了什么
- 没有发现隐患时hazards返回空数组
- 只输出JSON，不输出任何前缀或说明文字"""

ALLOWED_TYPES = {"image/jpeg", "image/png", "image/gif", "image/webp"}


@app.post("/analyze")
async def analyze(file: UploadFile = File(...)):
    if file.content_type not in ALLOWED_TYPES:
        raise HTTPException(400, "仅支持 JPG/PNG/GIF/WebP 格式")

    contents = await file.read()
    if len(contents) > 20 * 1024 * 1024:
        raise HTTPException(400, "图片不能超过 20MB")

    api_key = os.environ.get("DASHSCOPE_API_KEY")
    if not api_key:
        raise HTTPException(500, "未配置 DASHSCOPE_API_KEY，请检查环境变量")

    media_type = file.content_type or "image/jpeg"
    b64 = base64.standard_b64encode(contents).decode("utf-8")

    client = OpenAI(
        api_key=api_key,
        base_url="https://dashscope.aliyuncs.com/compatible-mode/v1",
    )

    response = client.chat.completions.create(
        model="qwen-vl-max",
        max_tokens=2000,
        messages=[
            {"role": "system", "content": SYSTEM_PROMPT},
            {
                "role": "user",
                "content": [
                    {
                        "type": "image_url",
                        "image_url": {"url": f"data:{media_type};base64,{b64}"},
                    },
                    {"type": "text", "text": USER_PROMPT},
                ],
            },
        ],
    )

    text = response.choices[0].message.content.strip()
    # 清洗 markdown 代码块
    if text.startswith("```"):
        parts = text.split("```")
        if len(parts) >= 2:
            text = parts[1]
            if text.startswith("json"):
                text = text[4:]
        text = text.strip()

    try:
        result = json.loads(text)
    except json.JSONDecodeError:
        raise HTTPException(500, f"模型返回格式异常：{text[:300]}")

    # 兼容旧格式 unconfirmed（字符串数组 → 对象数组）
    if result.get("unconfirmed") and isinstance(result["unconfirmed"][0], str):
        result["unconfirmed"] = [
            {"observation": s, "suggestion": "建议人工复核"} for s in result["unconfirmed"]
        ]

    return JSONResponse(result)


app.mount("/", StaticFiles(directory="static", html=True), name="static")
